{
  "name": "mainline-immunity-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/smoke.test.js",
    "lint": "node -e \"process.exit(0)\"",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "run:scenario": "node dist/cli/run-scenario.js",
    "run:pipeline": "node dist/cli/run-pipeline.js",
    "run:server": "node dist/cli/run-server.js",
    "start": "node dist/cli/run-server.js"
  },
  "dependencies": {
    "ws": "^8.18.1"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "@types/ws": "^8.5.14",
    "typescript": "^5.9.3"
  }
}
